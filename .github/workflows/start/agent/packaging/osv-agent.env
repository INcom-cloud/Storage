# /etc/osv-agent.env — osv-agent 기본 환경설정
# RPM 은 이 파일을 %config(noreplace) 로 설치 → 업데이트 시 사용자 수정 보존.
# Ansible(AWX) 로 노드별 값을 덮어쓸 수 있음.
T2_API_URL=http://t2-api:8000
T2_API_KEY=your-secret-key
OSV_API_TIMEOUT=30
COLLECTOR_INTERVAL=3600
LOG_LEVEL=INFO
